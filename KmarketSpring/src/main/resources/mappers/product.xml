<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace = mapper파일의 아이디 값 -->
<mapper namespace="kr.co.kmarket.dao.ProductDAO">

	<select id="selectProduct" resultType="kr.co.kmarket.vo.ProductVO">
		SELECT * FROM `km_product` ORDER BY #{param} DESC LIMIT 8;
	</select>
	<select id="selectProducts1" resultType="kr.co.kmarket.vo.ProductVO">
		SELECT * FROM `km_product` WHERE `cate1`=#{cate1} ORDER BY #{sold} DESC;
	</select>
	<select id="selectProducts2" resultType="kr.co.kmarket.vo.ProductVO">
		SELECT * FROM `km_product` WHERE `cate1`=#{cate1} AND `cate2`=#{cate2} ORDER BY #{sold} DESC;
	</select>
</mapper>